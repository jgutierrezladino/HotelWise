rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
  	match /{document=**} {
      allow write: if request.time < timestamp.date(2024, 3, 16);
      allow write: if request.auth.uid != null;
      allow read: if true;
    }
  }
}